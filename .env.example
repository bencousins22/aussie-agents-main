# This file contains environment variables that are used by the application.
# Copy this file to .env and replace the values with your own.

# The base URL of the API.
# For local development with a local backend, this can be a local URL.
# For connecting to a remote backend (e.g., via ngrok), use the ngrok URL.
VITE_API_URL=http://localhost:8000

# The API key for authenticating with the backend.
# This may not be required for local development.
VITE_API_KEY=
